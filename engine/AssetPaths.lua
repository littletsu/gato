local assetsPath = "/assets"

local paths = {
    bgm = assetsPath .. "/BGM/",
    fx = assetsPath .. "/FX/",
    fonts = assetsPath .. "/Fonts/",
    sprites = assetsPath .. "/Sprites/",
    ui = assetsPath .. "/UI/"
}

return paths
