local assetsPath = "/assets"

local paths = {
    bgm = assetsPath .. "/BGM/",
    backgrounds = assetsPath .. "/Backgrounds/",
    dialogs = assetsPath .. "/Dialogs/",
    fx = assetsPath .. "/FX/",
    fonts = assetsPath .. "/Fonts/",
    shaders = assetsPath .. "/Shaders/",
    sprites = assetsPath .. "/Sprites/",
    ui = assetsPath .. "/UI/"
}

return paths
